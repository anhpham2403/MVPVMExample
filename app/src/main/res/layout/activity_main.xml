<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <variable
            name="viewModel"
            type="com.example.anh.mvpvm.screen.main.MainViewModel"
            ></variable>
        <variable name="view" type="android.view.View"/>

    </data>
    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.fstyle.structure_android.screen.main.MainActivity"
        >


        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="?android:attr/progressBarStyle"
            android:visibility="@{viewModel.complete ? view.GONE : view.VISIBLE}"
            android:layout_marginRight="8dp"
            app:layout_constraintRight_toRightOf="@+id/recycler_view"
            android:layout_marginLeft="8dp"
            app:layout_constraintLeft_toLeftOf="@+id/recycler_view"
            app:layout_constraintTop_toTopOf="@+id/recycler_view"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toBottomOf="@+id/recycler_view"
            android:layout_marginBottom="8dp"
            />
        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginLeft="8dp"
            android:layout_marginStart="8dp"
            app:layoutManager="LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="1.0"
            app:recyclerAdapter="@{viewModel.userAdapter}"
            >
        </android.support.v7.widget.RecyclerView>

    </android.support.constraint.ConstraintLayout>
</layout>

